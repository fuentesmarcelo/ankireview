// PREP

// Your task is to return the correct string using the Template String Feature.

var templateStrings = function(noun, adjective) {
    return //complete this
}

// P: the parameter is a noun and adjective
// R: we want to return the string with template string feature
// E: n/a
// P: return with the back ticks and template literals


// my solution

var templateStrings = function(noun, adjective) {
    return `${noun} + ${adjective}`
}