//Create a function that takes in a string
//Alert if the string is a palindrome or not

// hint: split, reverse, join, arrow function


// the parameter is going to be a string
// we want to alert the string if its a palindrome or not


const isPal = str => str.split('').reverse().join('');