// PREP
// We need a function that can transform a number (integer) into a string.

function numberToString(num) {
    // Return a string of the number here!
}
hint: .toString();

// P: the parameter is a number
// R: we would return the string to number 
// E: 'str' => 24
// P: we are going to use toString method to turn the parameter from a number to a string

function numberToString(num){
    return num.toString();
}