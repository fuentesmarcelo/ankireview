// PREP
//Create a function that takes an integer as an argument and returns "Even" for even numbers or "Odd" for odd numbers.

hint: if or arrow modular

// P: the parameter is going to be an integer
// R: we want to return 'even' for even nums or 'odd' for odd nums
// E: n/a
// P: make an arrow function with a boolean

// my solution

const evenOrOdd = num => num % 2 === 0 ? 'even' : 'odd';