// PREP

// Create a function that takes in 1 number. Console log the cube root of the number. Call the function.

hint: Math.cbrt()


// P: the parameter is going to be a numbeer
// R: we want to return the number's cube root


// my solution

function numToCube(num){
    let cubeNum = num.Math.cbrt(num);
    console.log(cubeNum)
}

// right solution

function cubeRootIt(n1){
    console.log(Math.cbrt(n1))
}