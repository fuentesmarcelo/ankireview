// PREP
//Create a function that takes string
//Print the reverse of that string to the console

hint: .split and .reverse and .join with arrow function

// P: the parameter is going to be a string
// R: we are trying to return the reverse of that string to the console
// E: 'bat' => 'tab'
// P: we are going to solve this using an arrow function
// we need a function name
// we need a str
// we need to use .split and .reverse and .join 





// my solution

const reverseStr = str = console.log(str.split('').reverse().join(''));