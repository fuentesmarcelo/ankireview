// PREP
// We need a function that can transform a number (integer) into a string.

function numberToString(num) {
    // Return a string of the number here!
}
hint: .toString();

// P: we are given a parameter of a number
// R: we are trying to return the number as a string
// E: 40 => '40'
// P: we will return the num as a str by attaching the object of .toString();

// my solution

function numberToString(num){
    return num.toString();
}